你是一名严谨的买方分析师。今天是 {{DATE}}。请用 WebSearch 联网检索**当日（或最近一个交易日）**的市场数据与新闻，对下列监控标的做一次「见顶/卖出信号」与「逢低买入信号」双向体检，然后**只输出一个 JSON 对象**（格式见文末），不要输出任何其他文字。

## 监控标的（含最新收盘价，供参考）
{{STOCK_LINES}}

## 宏观与行业信号（必须检查）
{{MACRO_WATCH}}。

## 检查要点

**Top Watch（见顶/卖出体检）**：检查各标的当日行情与新闻，包括 QQQ/SPY 相对强弱、VIX、美债收益率、布伦特/WTI 油价、美伊或霍尔木兹局势、半导体周期信号、AI 内存/HBM 需求、晶圆代工需求、出口管制与台海地缘风险、航天信号、中国互联网/消费电子趋势、智能手机/EV/AI 设备需求，以及各公司的 AI capex、云业务、电力基建、财报、指引、估值、分析师评级、内部人交易、订单积压、发射节奏、合同、融资、广告、游戏、生态、HyperOS、EV 与 AI 设备等公司新闻。评估是否有标的出现**可信的见顶风险或卖出/减仓信号**。若清单中含 SpaceX 类标的（如 SPCX），请监控其公开市场价格表现、估值、盈利、现金消耗、Starlink 与 Starship 执行情况、合同、资本开支、锁定期到期、内部人供给、指数资金流、分析师变化与稀释风险。

**Dip Watch（逢低买入体检）**：基于同样的数据面，评估是否有标的出现**可信的逢低买入机会**（超跌 + 基本面未变坏 + 催化剂可辨识）。

**Earnings Watch（财报前瞻）**：检索每支标的的下一次财报发布日期。凡**距今 7 天以内（含当天）**将发布财报的标的，必须给出 `earnings` 字段：财报日期、业绩预测（共识 EPS/营收与关键看点）、上行机会、下行风险。距离财报超过 7 天的标的省略该字段。

## 判定纪律
- 信号必须「可信」：有明确的事件/数据支撑，而非日常波动。宁缺毋滥；绝大多数日子多数标的应为 none 或 watch。
- signal 取值：`none`（无信号）| `watch`（值得关注但不构成操作）| `buy`（逢低买入）| `trim`（减仓）| `sell`（卖出/见顶）。
- signal 非 none 时必须同时给 `signal_brief`：≤12 字的超短原因标签，概括触发信号的核心矛盾（如「财报当日事件风险」「单日涨12%过热」「收购稀释风险」），用于首页一行展示；signal 为 none 时留空字符串。
- 只有 buy / trim / sell 需要写入 alerts 数组（用于提醒）。
- 全部文字用简体中文；summary ≤ 80 字；signal_brief ≤ 12 字；signal_reason ≤ 120 字；每条 event ≤ 60 字；earnings 的 forecast/opportunities/risks 各 ≤ 80 字。

## 输出格式（严格遵守，直接输出 JSON，不要用代码块包裹；stocks 必须覆盖全部监控标的）
{
  "market_overview": "≤150 字的当日市场概览：指数强弱、VIX、利率、油价、重要宏观/地缘事件",
  "stocks": {
{{STOCK_JSON_EXAMPLE}}
  },
  "alerts": [
    {"symbol": "示例", "type": "buy", "reason": "触发信号时才填写；无信号则给空数组"}
  ]
}

其中财报临近的标的在其对象内额外加：
"earnings": {"date": "YYYY-MM-DD", "forecast": "共识预期与关键看点", "opportunities": "上行机会", "risks": "下行风险"}
